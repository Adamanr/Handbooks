---
sidebar_position: 18
description: "В этом уроке мы изучим работу с пакетами, модулями и организацией проекта."
---

# Пакеты, модули и организация проекта

Привет! Десятый урок — про то, как писать **настоящий, профессиональный** код на Go. До этого мы писали всё в одном файле или небольшой папке. Теперь научимся структурировать большие проекты, использовать пакеты, управлять зависимостями и следовать лучшим практикам.

Это то, что отличает хобби-проект от production-ready приложения.

Готовы? Давайте строить серьёзный проект!

## Пакеты

Пакет — это папка с Go-файлами, имеющими одинаковое объявление `package`.

### Правила экспорта

- **Заглавная буква** → экспортируется (доступна извне пакета)
- **строчная буква** → приватная (только внутри пакета)

```go
// pkg/mathutils/add.go
package mathutils

func Add(a, b int) int { // экспортируется
    return a + b
}

func secretHelper(x int) int { // приватная
    return x * 2
}
```

```go
// main.go
import "myproject/pkg/mathutils"

func main() {
    mathutils.Add(3, 4)        // OK
    // mathutils.secretHelper(5) // ошибка компиляции
}
```

### `internal` — приватные пакеты

Папка `internal/` — код, который **нельзя** импортировать извне модуля.

```
myproject/
├── internal/
│   └── database/     // только мой проект может импортировать
└── pkg/
    └── api/          // можно импортировать извне
```

### `init()` — автоматическая инициализация

Функция `init()` вызывается автоматически при импорте пакета (можно несколько).

```go
// internal/config/config.go
package config

import "log"

var AppConfig Config

func init() {
    slog.Info("Конфигурация инициализируется...")
    // загрузка из env, файла и т.д.
}
```

## Модули (go.mod)

С Go 1.11 появилась система модулей — современный способ управления зависимостями.

### Инициализация модуля

```bash
go mod init github.com/ваш-ник/myproject
```

Создаст `go.mod`:

```mod
module github.com/ваш-ник/myproject

go 1.23
```

### Добавление зависимостей

```bash
go get github.com/gin-gonic/gin@latest
go get github.com/lib/pq
go get github.com/joho/godotenv
```

`go.mod` обновится, появится `go.sum` с хешами.

### Полезные команды

```bash
go mod tidy          # удалить неиспользуемые, добавить недостающие
go mod vendor        # скопировать зависимости в vendor/ (для старых систем)
go list -m all       # список всех зависимостей
go mod why pkg       # зачем нужен этот пакет
```

## Рекомендуемая структура проекта

```text
myproject/
├── go.mod
├── go.sum
├── cmd/                     # Точки входа (исполняемые программы)
│   └── server/
│       └── main.go          # HTTP-сервер
├── internal/                # Приватный код (не импортируется извне)
│   ├── config/              # Загрузка конфигурации
│   ├── database/            # Подключение к БД, репозитории
│   ├── api/                 # HTTP-обработчики, маршруты
│   ├── service/             # Бизнес-логика
│   └── models/              # Структуры данных
├── pkg/                     # Публичные пакеты (можно переиспользовать)
│   └── logger/              # Кастомный логгер
├── scripts/                 # Скрипты (миграции, генерация)
├── configs/                 # config.json, .env.example
├── migrations/              # SQL-миграции
├── test/                    # Интеграционные тесты
├── Dockerfile
├── docker-compose.yml
└── README.md
```

### Пример: слой конфигурации

```go
// internal/config/config.go
package config

import (
    "encoding/json"
    "os"
    "sync"
)

type Config struct {
    Server struct {
        Port int    `json:"port"`
        Host string `json:"host"`
    } `json:"server"`
    Database struct {
        DSN string `json:"dsn"`
    } `json:"database"`
}

var (
    instance *Config
    once     sync.Once
)

func Load(path string) error {
    var err error
    once.Do(func() {
        data, readErr := os.ReadFile(path)
        if readErr != nil {
            err = readErr
            return
        }

        instance = &Config{}
        err = json.Unmarshal(data, instance)
    })
    return err
}

func Get() *Config {
    if instance == nil {
        panic("конфигурация не загружена!")
    }
    return instance
}
```

### Пример: слой базы данных

```go
// internal/database/database.go
package database

import (
    "database/sql"
    "sync"

    _ "github.com/lib/pq"
)

var (
    db   *sql.DB
    once sync.Once
)

func Connect(dsn string) error {
    var err error
    once.Do(func() {
        db, err = sql.Open("postgres", dsn)
        if err != nil {
            return
        }
        err = db.Ping()
    })
    return err
}

func GetDB() *sql.DB {
    return db
}
```

### Пример: HTTP-сервер (cmd/server/main.go)

```go
// cmd/server/main.go
package main

import (
    "log"
    "net/http"

    "github.com/gin-gonic/gin"
    "myproject/internal/api"
    "myproject/internal/config"
    "myproject/internal/database"
)

func main() {
    if err := config.Load("configs/config.json"); err != nil {
        log.Fatal(err)
    }

    if err := database.Connect(config.Get().Database.DSN); err != nil {
        log.Fatal(err)
    }

    r := gin.Default()
    api.RegisterRoutes(r)

    slog.Info("Сервер запущен", "адрес", config.Get().Server.Port)
    log.Fatal(http.ListenAndServe(":8080", r))
}
```

## Лучшие практики

1. **Именование пакетов** — короткие, в нижнем регистре, без `utils`, `helpers`, `manager`
   - Хорошо: `config`, `database`, `api`, `service`
   - Плохо: `myutils`, `userManager`

2. **Экспортируйте осознанно** — только то, что действительно нужно снаружи

3. **Документируйте экспортируемые сущности**

   ```go
   // Add складывает два числа и возвращает результат.
   // Пример: Add(2, 3) => 5
   func Add(a, b int) int {
       return a + b
   }
   ```

4. **Используйте `internal/`** для кода, который не должен утекать наружу

5. **Разделяйте слои**:
   - `api` — HTTP, валидация запросов
   - `service` — бизнес-логика
   - `database` — работа с БД
   - `models` — структуры данных

### Примерная структура проекта в итоге
```
myapi/
├── cmd
│   ├── main.go
├── internal
│   ├── config
│   │   ├── config.go
│   │   └── config.go.mod
│   ├── users
│   │   ├── users.go
│   │   └── users.go.mod
├── go.mod
├── go.sum
├── .env
```


## Упражнения


### 1. **Запуск миссии — инициализация проекта**  
   Создай папку `space-config-app` и выполни в терминале команды:  
   ```bash
   go mod init space-config-app
   go get github.com/joho/godotenv
   ```


   Создай файл `main.go` и в нём сделай вывод:  
   ```
   🚀 Миссия "Конфигурация космической станции" запущена!
   Модуль инициализирован: space-config-app
   Зависимость godotenv добавлена — готовимся к загрузке секретных данных!
   ```

### 2. **Секретный файл конфигурации станции (.env)**  
   Создай файл `.env` в корне с данными миссии:  
   ```
   STATION_NAME=Альфа-7
   DEBUG=true
   MAX_CREW=12
   WELCOME_MESSAGE=Добро пожаловать на борт, космонавт!
   MISSION_TARGET=Марс
   LAUNCH_YEAR=2030
   ```

   Добавь комментарий в файл: `# Секретные параметры космической станции`

### 3. **Загрузка конфигурации — активация систем станции**  
   В `main.go` загрузи `.env` и выведи красивый стартовый экран:  
   ```
   🌌 Системы станции активированы!
   Название станции: Альфа-7
   Режим отладки: включён 🔧
   Максимальный экипаж: 12 человек
   Цель миссии: Марс 🚀
   Год запуска: 2030
   Приветствие: Добро пожаловать на борт, космонавт!
   ```

    Пример работы с библиотекой godotenv: 

   ```go
    package main

    import (
        "log"
        "os"

        "github.com/joho/godotenv"
    )

    func main() {
        if err := godotenv.Load(); err != nil {
            panic("Error loading .env file")
        }

        s3Bucket := os.Getenv("S3_BUCKET")
        secretKey := os.Getenv("SECRET_KEY")
    }
   ```

### 4. **Структура конфигурации — чертёж станции**  
   Создай структуру `StationConfig`:  
   ```go
   type StationConfig struct {
       Name          string
       Debug         bool
       MaxCrew       int
       Welcome       string
       Target        string
       LaunchYear    int
   }
   ```

   Напиши функцию `LoadConfig() (StationConfig, error)`, которая:  
   - Читает переменные из окружения  
   - Парсит `DEBUG` в bool и числа  
   - Возвращает заполненную структуру  

   В `main` выведи:  
   ```
   📋 Чертёж станции загружен успешно!
   ```

### 5. **Экипаж станции — список космонавтов**  
   Создай структуру `Astronaut` с полями:  
   - `ID int`  
   - `Name string`  
   - `Role string` (например, "Командир", "Инженер", "Учёный")  

   В `main` создай слайс из 5–6 космонавтов.  
   Выведи список экипажа в таблице:  
   ```
   👨‍🚀 Экипаж станции Альфа-7:
   1. Иван Петров     — Командир
   2. Мария Сидорова  — Пилот
   3. Алексей Козлов  — Инженер
   4. Ольга Иванова   — Учёный-биолог
   5. Дмитрий Соколов — Врач
   ```

### 6. **Сохранение и загрузка экипажа — журнал полёта**  
   Напиши функции:  
   - `SaveCrewToJSON(filename string, crew []Astronaut) error`  
   - `LoadCrewFromJSON(filename string) ([]Astronaut, error)`  

   В `main`:  
   - Сохрани экипаж в `crew.json`  
   - Загрузи обратно  
   - Выведи:  
     ```
     💾 Журнал экипажа сохранён в crew.json
     📂 Журнал загружен обратно — все на борту!
     Сравнение: экипаж идентичен ✅
     ```

### 7. **Бонус: умная валидация конфигурации — система безопасности**  
   В `LoadConfig()` добавь проверки с значениями по умолчанию:  
   - Если `MAX_CREW` ≤ 0 → установи 6 и выведи "⚠️  Автоматическая коррекция: экипаж увеличен до 6"  
   - Если `LAUNCH_YEAR` < 2026 → "🚨 ОШИБКА: Год запуска слишком ранний! Установлен 2030"  
   - Если `STATION_NAME` пустое → "Default Station"  

   Выведи финальный отчёт:  
   ```
   🛡️ Система безопасности завершила проверку конфигурации
   Все параметры в норме (или исправлены автоматически)
   Станция готова к запуску! 🚀
   ```
