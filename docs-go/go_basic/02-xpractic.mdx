---
sidebar_position: 3
description: "Практические задания по основам синтаксиса Go: переменные, типы данных, операторы, указатели."
---

# Практические задания: Основы синтаксиса

import { useState } from 'react';
import { CodeBlock } from '../../src/components/GoPlayground'

export const TestCodeBlock = ({ code, language }) => {
    return (
        <div>

        </div>
    );
};

export const RandomVariantButton = ({ variants, onSelect, selectedVariant }) => {
    const handleRandom = () => {
        const randomIndex = Math.floor(Math.random() * variants.length);
        onSelect(randomIndex);
    };

    return (
        <div style={{ marginBottom: '1rem' }}>
            <button
                onClick={handleRandom}
                style={{
                    padding: '0.5rem 1rem',
                    backgroundColor: '#0070f3',
                    color: 'white',
                    border: 'none',
                    borderRadius: '6px',
                    cursor: 'pointer',
                    fontWeight: '500',
                    fontSize: '0.95rem',
                }}
            >
                🎲 Выбрать случайный вариант
            </button>
            {selectedVariant !== null && (
                <span style={{ marginLeft: '1rem', color: '#0070f3', fontWeight: '500' }}>
                    Выбран вариант {selectedVariant + 1}
                </span>
            )}


        </div>
    );
};

export const TaskWithVariants = ({ title, description, variants, children }) => {
    const [selectedVariant, setSelectedVariant] = useState(null);

    return (
        <div style={{
            border: '1px solid #e0e0e0',
            borderRadius: '8px',
            padding: '1.5rem',
            marginBottom: '2rem',
            backgroundColor: '#fafafa'
        }}>
            <h3 style={{ marginTop: 0 }}>{title}</h3>
            <p style={{ color: '#555' }}>{description}</p>

            <RandomVariantButton
                variants={variants}
                onSelect={setSelectedVariant}
                selectedVariant={selectedVariant}
            />

            {selectedVariant !== null && (
                <div style={{
                    backgroundColor: 'black',
                    padding: '1.5rem',
                    borderRadius: '6px',
                    border: '2px solid #0070f3',
                    marginTop: '1rem'
                }}>
                    <h4 style={{ marginTop: 0, color: '#0070f3' }}>
                        Вариант {selectedVariant + 1}
                    </h4>
                    {variants[selectedVariant]}
                </div>
            )}

            <br />

            {children}
        </div>
    );
};

## Компилятор для работы с кодом

В данном компиляторе вы можете запускать код на языке Go и видеть результаты выполнения в реальном времени. Это полезно для проверки и отладки кода, а также для изучения работы синтаксиса и стандартной библиотеки языка.

<CodeBlock />

## Задание 1: Работа с переменными и типами

<TaskWithVariants
  title="Задание 1: Информация о товаре"
  description="Создайте программу для хранения и вывода информации о товаре в интернет-магазине."
  variants={[
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Название товара: "Смартфон Samsung Galaxy"</li>
        <li>Цена: 45999.99 руб.</li>
        <li>Количество на складе: 15 шт.</li>
        <li>В наличии: да</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Используйте правильные типы данных для каждого поля</li>
        <li>Примените короткое объявление переменных (<code>:=</code>)</li>
        <li>Выведите информацию в читаемом формате с помощью <code>fmt.Printf</code></li>
        <li>Вычислите общую стоимость всех товаров на складе</li>
      </ul>
    </>,
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Название товара: "Ноутбук ASUS"</li>
        <li>Цена: 89990.50 руб.</li>
        <li>Количество на складе: 7 шт.</li>
        <li>В наличии: да</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Используйте правильные типы данных для каждого поля</li>
        <li>Примените короткое объявление переменных (<code>:=</code>)</li>
        <li>Выведите информацию в читаемом формате с помощью <code>fmt.Printf</code></li>
        <li>Вычислите общую стоимость всех товаров на складе</li>
      </ul>
    </>
  ]}
>
  <details>
    <summary>💡 Подсказка</summary>
    <p>Для цены используйте <code>float64</code>, для количества — <code>int</code>, для флага наличия — <code>bool</code>.</p>
    <p>Формат вывода: <code>fmt.Printf("Товар: %s\nЦена: %.2f руб.\n", name, price)</code></p>
  </details>
</TaskWithVariants>

## Задание 2: Преобразование типов

<TaskWithVariants
  title="Задание 2: Калькулятор скидок"
  description="Напишите программу для расчёта цены товара с учётом скидки."
  variants={[
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Исходная цена: 5000 руб. (целое число)</li>
        <li>Скидка: 15%</li>
        <li>Посчитайте итоговую цену и выведите её</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Цена изначально хранится как <code>int</code></li>
        <li>Преобразуйте в <code>float64</code> для расчёта скидки</li>
        <li>Выведите: исходную цену, размер скидки в рублях, итоговую цену</li>
        <li>Округлите итоговую цену обратно до целого числа (<code>int</code>)</li>
      </ul>
    </>,
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Исходная цена: 12000 руб. (целое число)</li>
        <li>Скидка: 20%</li>
        <li>Посчитайте итоговую цену и выведите её</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Цена изначально хранится как <code>int</code></li>
        <li>Преобразуйте в <code>float64</code> для расчёта скидки</li>
        <li>Выведите: исходную цену, размер скидки в рублях, итоговую цену</li>
        <li>Округлите итоговую цену обратно до целого числа (<code>int</code>)</li>
      </ul>
    </>,
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Исходная цена: 8500 руб. (целое число)</li>
        <li>Скидка: 25%</li>
        <li>Посчитайте итоговую цену и выведите её</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Цена изначально хранится как <code>int</code></li>
        <li>Преобразуйте в <code>float64</code> для расчёта скидки</li>
        <li>Выведите: исходную цену, размер скидки в рублях, итоговую цену</li>
        <li>Округлите итоговую цену обратно до целого числа (<code>int</code>)</li>
      </ul>
    </>
  ]}
>
  <details>
    <summary>💡 Подсказка</summary>
    <p>Преобразование: <code>priceFloat := float64(price)</code></p>
    <p>Скидка: <code>discount := priceFloat * (discountPercent / 100)</code></p>
    <p>Обратно в int: <code>finalPrice := int(priceFloat - discount)</code></p>
  </details>
</TaskWithVariants>

## Задание 3: Работа со строками

<TaskWithVariants
  title="Задание 3: Генератор приветствий"
  description="Создайте программу, которая формирует персонализированное приветствие."
  variants={[
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Имя: "Александр"</li>
        <li>Возраст: 28</li>
        <li>Город: "Москва"</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Используйте <code>strings.Builder</code> для формирования сообщения</li>
        <li>Преобразуйте возраст из числа в строку с помощью <code>strconv</code></li>
        <li>Сформируйте приветствие вида: "Привет, Александр! Тебе 28 лет. Ты живёшь в городе Москва. 🎉"</li>
        <li>Посчитайте и выведите количество символов (рун) и байтов в итоговой строке</li>
      </ul>
    </>
  ]}
>
  <details>
    <summary>💡 Подсказка</summary>
    <p>Используйте <code>builder.WriteString()</code> для добавления частей строки.</p>
    <p>Длина в байтах: <code>len(str)</code></p>
    <p>Длина в символах: <code>utf8.RuneCountInString(str)</code></p>
  </details>
</TaskWithVariants>

## Задание 4: Работа с byte и rune

<TaskWithVariants
  title="Задание 4: Анализатор текста"
  description="Напишите программу для анализа строки с различными символами."
  variants={[
    <>
      <p><strong>Условие:</strong></p>
      <p>Дана строка: <code>"Go — это круто! 🚀"</code></p>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Выведите длину строки в байтах</li>
        <li>Выведите длину строки в символах (рунах)</li>
        <li>Переберите строку с помощью <code>range</code> и выведите каждый символ с его кодом Unicode</li>
        <li>Найдите и выведите позицию (в байтах) эмодзи ракеты 🚀</li>
        <li>Преобразуйте строку в <code>[]rune</code> и выведите последний символ</li>
      </ul>
    </>,
    <>
      <p><strong>Условие:</strong></p>
      <p>Дана строка: <code>"Привет, мир! 👋"</code></p>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Выведите длину строки в байтах</li>
        <li>Выведите длину строки в символах (рунах)</li>
        <li>Переберите строку с помощью <code>range</code> и выведите каждый символ с его кодом Unicode</li>
        <li>Найдите и выведите позицию (в байтах) эмодзи руки 👋</li>
        <li>Преобразуйте строку в <code>[]rune</code> и выведите последний символ</li>
      </ul>
    </>
  ]}
>
  <details>
    <summary>💡 Подсказка</summary>
    <p>Перебор: <code>for i, r := range str &#123; fmt.Printf("Позиция %d: %c (U+%04X)\n", i, r, r) &#125;</code></p>
    <p>Последний символ: <code>runes := []rune(str); lastChar := runes[len(runes)-1]</code></p>
  </details>
</TaskWithVariants>

## Задание 5: Операторы и логика

<TaskWithVariants
  title="Задание 5: Проверка прав доступа"
  description="Создайте систему проверки прав доступа пользователя."
  variants={[
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Возраст пользователя: 19</li>
        <li>Есть подписка: true</li>
        <li>Статус: "premium"</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Доступ разрешён, если: возраст ≥ 18 И (есть подписка ИЛИ статус == "premium")</li>
        <li>Выведите результат проверки и причину решения</li>
        <li>Используйте логические операторы (<code>&&</code>, <code>||</code>, <code>!</code>)</li>
        <li>Посчитайте "уровень доступа" по формуле: базовый уровень = 1, +1 за подписку, +2 за premium статус</li>
      </ul>
    </>,
    <>
      <p><strong>Условие:</strong></p>
      <ul>
        <li>Возраст пользователя: 16</li>
        <li>Есть подписка: false</li>
        <li>Статус: "basic"</li>
      </ul>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Доступ разрешён, если: возраст ≥ 18 И (есть подписка ИЛИ статус == "premium")</li>
        <li>Выведите результат проверки и причину решения</li>
        <li>Используйте логические операторы (<code>&&</code>, <code>||</code>, <code>!</code>)</li>
        <li>Посчитайте "уровень доступа" по формуле: базовый уровень = 1, +1 за подписку, +2 за premium статус</li>
      </ul>
    </>
  ]}
>
  <details>
    <summary>💡 Подсказка</summary>
    <p>Логика: <code>hasAccess := age >= 18 && (hasSubscription || status == "premium")</code></p>
    <p>Уровень доступа можно накапливать через <code>+=</code></p>
  </details>
</TaskWithVariants>

## Задание 6: Указатели

<TaskWithVariants
  title="Задание 6: Система начисления бонусов"
  description="Реализуйте функции для работы с балансом бонусного счёта."
  variants={[
    <>
      <p><strong>Условие:</strong></p>
      <p>Начальный баланс: 100 бонусов</p>
      <p><strong>Требования:</strong></p>
      <ul>
        <li>Создайте функцию <code>addBonus(balance *int, amount int)</code>, которая добавляет бонусы</li>
        <li>Создайте функцию <code>spendBonus(balance *int, amount int) bool</code>, которая списывает бонусы (возвращает false, если недостаточно средств)</li>
        <li>Создайте функцию <code>getBalanceInfo(balance *int) string</code>, которая возвращает строку с информацией о балансе</li>
        <li>Выполните следующие операции:
          <ol>
            <li>Начислите 50 бонусов</li>
            <li>Попытайтесь потратить 30 бонусов</li>
            <li>Попытайтесь потратить 200 бонусов (должно быть отклонено)</li>
            <li>После каждой операции выводите текущий баланс</li>
          </ol>
        </li>
      </ul>
    </>
  ]}
>
  <details>
    <summary>💡 Подсказка</summary>
    <p>Работа с указателем: <code>*balance += amount</code></p>
    <p>Проверка в <code>spendBonus</code>: <code>if *balance >= amount &#123; *balance -= amount; return true &#125;</code></p>
    <p>Используйте <code>fmt.Sprintf</code> для формирования строки с балансом</p>
  </details>
</TaskWithVariants>

## Дополнительные материалы

После выполнения заданий рекомендуем:

1. **Экспериментировать**: попробуйте изменить значения переменных и посмотрите, как это влияет на результат
2. **Комбинировать**: объедините несколько заданий в одну программу
3. **Оптимизировать**: подумайте, как сделать код более эффективным или читаемым
4. **Документировать**: добавьте комментарии к своему коду

:::tip Совет
Если возникают ошибки компиляции, внимательно читайте сообщения от компилятора — Go обычно даёт очень понятные подсказки о том, что не так.
:::

:::info Следующий шаг
После выполнения всех заданий переходите к следующему уроку: **Управляющие конструкции** (условия, циклы, switch).
:::
