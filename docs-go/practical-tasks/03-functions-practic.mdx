---
sidebar_position: 5
description: "Практические задания по основам синтаксиса Go: переменные, типы данных, операторы, указатели."
---

# Практика: Функции

import { RandomVariantButton, TaskWithVariants, CodeBlockGo } from "../../src/components/practicModules/practic";

<CodeBlockGo />

## Задание 1: Простая функция с одним возвращаемым значением
<TaskWithVariants
  title="Задание 1: Вычисление площади фигуры"
  description="Напишите функцию, возвращающую площадь геометрической фигуры."
  variants={[
    <><p>Функция <code>RectangleArea(width, height float64) float64</code> — площадь прямоугольника. Вызов: 6.0 × 9.0</p></>,
    <><p>Функция <code>CircleArea(radius float64) float64</code> — площадь круга (π ≈ 3.14159). Вызов: r = 5.0</p></>,
    <><p>Функция <code>TriangleArea(base, height float64) float64</code> — площадь треугольника. Вызов: base=8.0, height=4.0</p></>,
    <><p>Функция <code>SquareArea(side float64) float64</code>. Вызов: side=10.0</p></>,
    <><p>Функция <code>TrapezoidArea(a, b, h float64) float64</code> — ((a+b)/2)*h. Вызов: a=5.0, b=8.0, h=6.0</p></>,
    <><p>Функция <code>ParallelogramArea(base, height float64) float64</code>. Вызов: base=12.0, height=5.0</p></>,
    <><p>Функция <code>RhombusArea(d1, d2 float64) float64</code> — (d1*d2)/2. Вызов: d1=10.0, d2=6.0</p></>,
    <><p>Функция <code>SectorArea(radius, angle float64) float64</code> — (angle/360)*π*r². Вызов: r=7.0, angle=90.0</p></>,
    <><p>Функция <code>EllipseArea(a, b float64) float64</code> — π*a*b. Вызов: a=4.0, b=7.0</p></>,
    <><p>Функция <code>RegularHexagonArea(side float64) float64</code> — (3√3/2)*side². Вызов: side=5.0</p></>,
    <><p>Функция <code>KiteArea(d1, d2 float64) float64</code> — (d1*d2)/2. Вызов: d1=8.0, d2=10.0</p></>,
    <><p>Функция <code>AnnulusArea(r1, r2 float64) float64</code> — π*(r1²-r2²). Вызов: r1=10.0, r2=5.0</p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Не забудьте импортировать <code>"math"</code> для <code>math.Pi</code> и других функций (например, <code>math.Tan</code>, <code>math.Sqrt</code>).</p>
        <p>Для вывода используйте <code>fmt.Printf("Площадь: %.2f\n", result)</code> — это даст красивый формат с двумя знаками после запятой.</p>
    </details>
</TaskWithVariants>

## Задание 2: Функция с несколькими возвращаемыми значениями
<TaskWithVariants
  title="Задание 2: Площадь и периметр"
  description="Функция возвращает площадь и периметр (или длину окружности)."
  variants={[
    <><p><code>RectMetrics(w, h float64) (float64, float64)</code> — площадь и периметр прямоугольника. Вызов: 5.0 × 8.0</p></>,
    <><p><code>CircleMetrics(r float64) (float64, float64)</code> — площадь и длина окружности. Вызов: r=10.0</p></>,
    <><p><code>SquareMetrics(s float64) (float64, float64)</code>. Вызов: s=7.0</p></>,
    <><p><code>TriangleMetrics(a, b, c float64) (float64, float64)</code> — площадь по Герону и периметр. Вызов: 3,4,5</p></>,
    <><p><code>TrapezoidMetrics(a, b, h float64) (float64, float64)</code> — площадь и сумма оснований как «периметр». Вызов: a=4, b=9, h=6</p></>,
    <><p><code>ParallelogramMetrics(base, side, h float64) (float64, float64)</code>. Вызов: base=10, side=6, h=5</p></>,
    <><p><code>RhombusMetrics(side float64) (float64, float64)</code> — площадь через высоту (придумайте) и периметр. Вызов: side=8.0</p></>,
    <><p><code>EquilateralTriangleMetrics(side float64) (float64, float64)</code>. Вызов: side=6.0</p></>,
    <><p><code>RegularPentagonMetrics(side float64) (float64, float64)</code> — площадь и периметр. Вызов: side=5.0</p></>,
    <><p><code>RectangleDiagonal(w, h float64) (float64, float64)</code> — площадь и диагональ. Вызов: 6.0 × 8.0</p></>,
  ]}
>

  <details>
      <summary>💡 Подсказка</summary>
      <p>Используйте именованные возвращаемые значения: <code>(area, perimeter float64)</code>. Тогда можно присваивать им значения в теле функции и завершить <code>return</code> без аргументов (голый return).</p>
      <p>Для формулы Герона: полупериметр s = (a+b+c)/2, площадь = √(s(s-a)(s-b)(s-c)).</p>
    </details>
</TaskWithVariants>

## Задание 3: Функция с указателями
<TaskWithVariants
  title="Задание 3: Изменение значения по указателю"
  description="Функция изменяет переданное значение."
  variants={[
    <><p><code>Double(v *int)</code> — удвоить. Начальное: 15 → 30</p></>,
    <><p><code>Triple(v *int)</code>. Начальное: 7 → 21</p></>,
    <><p><code>AddBonus(b *int, amount int)</code>. Начальное: 200, добавить 75 → 275</p></>,
    <><p><code>ApplyDiscount(p *float64, percent float64)</code>. Начальное: 1000.0, 20% → 800.0</p></>,
    <><p><code>Increment(v *int)</code> — +1. Вызвать 5 раз с начальным 0</p></>,
    <><p><code>Reset(v *int)</code> — обнулить. Начальное: 999 → 0</p></>,
    <><p><code>Multiply(v *float64, factor float64)</code>. Начальное: 10.0, ×3.5 → 35.0</p></>,
    <><p><code>Swap(a, b *int)</code>. Начальные: 42 и 13 → поменять местами</p></>,
    <><p><code>Scale(v *float64, scale float64)</code>. Начальное: 50.0, ×1.1 → 55.0</p></>,
    <><p><code>Negate(v *int)</code>. Начальное: 100 → -100</p></>,
    <><p><code>AddTax(p *float64, rate float64)</code>. Начальное: 800.0, +20% → 960.0</p></>,
    <><p><code>RoundToInt(v *float64)</code> — привести к ближайшему int (просто int(*v)). Начальное: 12.7 → 12.0</p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Чтобы изменить значение — разыменовывайте указатель: <code>*v = *v * 2</code> или <code>*v += amount</code>.</p>
        <p>В <code>main</code> передавайте адрес: <code>functionName(&variable)</code>. После вызова выводите изменённую переменную.</p>
    </details>
</TaskWithVariants>

## Задание 4: Вариадическая функция
<TaskWithVariants
  title="Задание 4: Вариадическая функция для вычислений"
  description="Функция принимает любое количество аргументов."
  variants={[
    <><p><code>Sum(nums ...int) int</code>. Тесты: 1,2,3,4 и пустой вызов</p></>,
    <><p><code>Product(nums ...float64) float64</code>. Тесты: 2.0, 3.0, 5.0</p></>,
    <><p><code>Average(nums ...float64) float64</code> (0 при пустом). Тесты: 10.0, 20.0, 30.0</p></>,
    <><p><code>MaxValue(nums ...int) int</code> (0 при пустом). Тесты: 5, 12, 8</p></>,
    <><p><code>MinValue(nums ...float64) float64</code>. Тесты: 3.5, 1.2, 7.8</p></>,
    <><p><code>TotalWithTax(items ...float64) float64</code> — сумма × 1.20</p></>,
    <><p><code>CountArgs(args ...string) int</code> — количество аргументов</p></>,
    <><p><code>Concat(strings ...string) string</code> — склеить без разделителя</p></>,
    <><p><code>SumEven(nums ...int) int</code> — сумма только чётных</p></>,
    <><p><code>MultiplyAll(factor float64, nums ...float64) float64</code></p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Внутри функции параметр <code>nums ...int</code> ведёт себя как слайс <code>[]int</code> — используйте <code>range</code> для перебора.</p>
        <p>Если аргументов нет — сумма/произведение/среднее должно быть 0 (или 1 для произведения).</p>
        <p>Тестируйте несколько вызовов в <code>main</code> с разным количеством аргументов.</p>
    </details>
</TaskWithVariants>

## Задание 5: defer для логирования
<TaskWithVariants
  title="Задание 5: Функция с defer-логом завершения"
  description="Добавьте defer с сообщением о завершении."
  variants={[
    <><p><code>SendEmail(to string)</code> → defer "Письмо отправлено на `to`"</p></>,
    <><p><code>GenerateInvoice(num int)</code> → defer "Счёт №`num` создан"</p></>,
    <><p><code>SaveUser(name string)</code> → defer "Пользователь `name` сохранён"</p></>,
    <><p><code>CalculateSalary(hours float64)</code> → defer "Зарплата рассчитана"</p></>,
    <><p><code>BackupDatabase(name string)</code> → defer "Бэкап `name` завершён"</p></>,
    <><p><code>DeployVersion(v string)</code> → defer "Версия `v` развёрнута"</p></>,
    <><p><code>RunTest(name string)</code> → defer "Тест `name` выполнен"</p></>,
    <><p><code>CompileCode(project string)</code> → defer "Проект `project` скомпилирован"</p></>,
    <><p><code>CloseSession(id int)</code> → defer "Сессия `id` закрыта"</p></>,
    <><p><code>ExportReport(title string)</code> → defer "Отчёт «`title`» экспортирован"</p></>,
    <><p><code>ValidateData(source string)</code> → defer "Данные из `source` проверены"</p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Используйте анонимную функцию в defer, чтобы передать параметры: <code>defer func() `{ fmt.Printf("Платёж %d обработан\n", id) }`()</code>.</p>
        <p>Если просто <code>defer fmt.Println(id)</code> — значение <code>id</code> будет взято на момент выполнения defer, а не объявления (может быть неожиданным).</p>
        <p>Вывод от defer появится в конце, после основного тела функции.</p>
    </details>
</TaskWithVariants>

## Задание 6: Замыкание — фабрика счётчиков
<TaskWithVariants
  title="Задание 6: Фабрика счётчиков через замыкание"
  description="Функция возвращает счётчик."
  variants={[
    <><p><code>MakeCounter(start int) func() int</code>. Тест: start=10 → 10,11,12</p></>,
    <><p><code>MakeCounterStep(start, step int) func() int</code>. Тест: start=0, step=5 → 0,5,10</p></>,
    <><p><code>MakeOrderID() func() int</code> — от 1000. Тест: 1000,1001,1002</p></>,
    <><p><code>MakeTimer(start float64) func() float64</code> — +0.1 каждый вызов</p></>,
    <><p><code>MakeBalance(start float64) func(float64) float64</code> — добавляет сумму</p></>,
    <><p><code>MakeMultiplier(factor float64) func(float64) float64</code></p></>,
    <><p><code>MakeIDWithPrefix(prefix string) func() string</code> — "user-1", "user-2"...</p></>,
    <><p><code>MakeDownCounter(start int) func() int</code> — уменьшает на 1</p></>,
    <><p><code>MakeScore(start int) func(int) int</code> — добавляет очки</p></>,
    <><p><code>MakeVersion(major, minor int) func() string</code> — "1.0", "1.1", ...</p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Внешняя функция создаёт переменную (например, <code>count := start</code>), а возвращает <code>func() int `{count += step; return count }`</code>.</p>
        <p>Каждый вызов фабрики создаёт независимое замыкание — свои переменные не пересекаются.</p>
        <p>В <code>main</code> создайте 2–3 счётчика и вызовите несколько раз, чтобы показать независимость.</p>
    </details>
</TaskWithVariants>

## Задание 7: Анонимные функции
<TaskWithVariants
  title="Задание 7: Анонимная функция с немедленным вызовом"
  description="Создайте и сразу выполните анонимную функцию."
  variants={[
    <><p>Анонимная функция, выводящая "Привет, мир!"</p></>,
    <><p>Анонимная функция, возвращающая квадрат числа и выводящая его</p></>,
    <><p>Анонимная функция, использующая внешнюю переменную name и приветствующая</p></>,
    <><p>Анонимная функция-калькулятор двух чисел</p></>,
    <><p>Анонимная функция, возвращающая строку с текущим временем (time.Now().String())</p></>,
    <><p>Анонимная функция, генерирующая случайное число (mathRand)</p></>,
    <><p>Анонимная функция, возвращающая сумму трёх чисел</p></>,
    <><p>Анонимная функция, преобразующая строку в верхний регистр</p></>,
    <><p>Анонимная функция, возвращающая длину строки</p></>,
    <><p>Анонимная функция, выводящая таблицу умножения на 7</p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Синтаксис немедленного вызова: <code>func(параметры) `{ тело }()`</code>.</p>
        <p>Если нужно использовать внешние переменные — они доступны внутри анонимной функции (замыкание).</p>
        <p>Для возврата значения присвойте в переменную: <code>`result := func(x int) int { return x * x }`(5)</code>.</p>
    </details>
</TaskWithVariants>

## Задание 8: Функции высшего порядка
<TaskWithVariants
  title="Задание 8: Функция, принимающая другую функцию"
  description="Реализуйте функцию высшего порядка."
  variants={[
    <><p><code>ApplyTwice(x int, f func(int) int) int</code> — применяет f дважды</p></>,
    <><p><code>ApplyNtimes(x int, n int, f func(int) int) int</code> — n раз</p></>,
    <><p><code>Transform(x float64, f func(float64) float64) float64</code></p></>,
    <><p><code>Operate(a, b float64, op func(float64, float64) float64) float64</code></p></>,
    <><p><code>Compose(f func(int) int, g func(int) int) func(int) int</code> — композиция</p></>,
    <><p><code>Repeat(str string, n int, f func(string) string) string</code></p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Тип функции как параметра: <code>func(int) int</code> или <code>func(float64, float64) float64</code>.</p>
        <p>В <code>main</code> передавайте анонимную функцию: <code>ApplyTwice(5, func(x int) int `{ return x + 1 }`)</code>.</p>
        <p>Для композиции: <code>return func(x int) int `{ return f(g(x)) }`</code>.</p>
    </details>
</TaskWithVariants>

## Задание 9: Named return + defer модификация
<TaskWithVariants
  title="Задание 9: Named return с изменением в defer"
  description="Используйте named return и defer для изменения результата."
  variants={[
    <><p><code>GetBonus() (total int)</code> → total = 100, defer total += 50 → 150</p></>,
    <><p><code>CalculateWithFee() (amount float64)</code> → amount = 1000, defer amount *= 1.05</p></>,
    <><p><code>ApplyDiscount() (price float64)</code> → price = 2000, defer price *= 0.9</p></>,
    <><p><code>GetScore() (points int)</code> → points = 50, defer points *= 2</p></>,
    <><p><code>FinalPrice() (cost float64)</code> → cost = 500, defer cost += 100 (доставка)</p></>,
    <><p><code>ProcessValue() (v int)</code> → v = 10, defer v = v * v</p></>,
    <><p><code>GetTaxed() (sum float64)</code> → sum = 800, defer sum *= 1.20</p></>,
    <><p><code>Compute() (result float64)</code> → result = 42.0, defer result = math.Sqrt(result)</p></>,
    <><p><code>FinalCount() (n int)</code> → n = 100, defer n--</p></>,
    <><p><code>GetFormatted() (text string)</code> → text = "hello", defer text = strings.ToUpper(text)</p></>,
  ]}
>
    <details>
        <summary>💡 Подсказка</summary>
        <p>Объявите <code>func Name() (result float64)</code>. Присвойте <code>result = 1000</code>, затем <code>defer func() `{ result *= 1.20 }()`</code>.</p>
        <p>Defer выполнится после вычисления return, но до фактического возврата — поэтому может изменить named переменные.</p>
        <p>Завершайте функцию просто <code>return</code>.</p>
    </details>
</TaskWithVariants>

## Задание 10: Комбинированное — всё вместе
<TaskWithVariants
  title="Задание 10: Полный мини-калькулятор"
  description="Объедините: variadic, указатели, несколько возвратов, defer, замыкание."
  variants={[
    <><p>Функция <code>CalcCart(total *float64, items ...float64) (float64, int)</code> + defer лог</p></>,
    <><p>Функция <code>UpdateBalance(b *float64, ops ...float64) float64</code> + defer "Баланс обновлён"</p></>,
    <><p>Функция <code>ProcessPayments(payments ...float64) (float64, float64)</code> — сумма и налог + defer</p></>,
    <><p>Замыкание + указатель: счётчик транзакций с балансом</p></>,
    <><p>Variadic + named return + defer модификация суммы</p></>,
    <><p>Функция с анонимной функцией внутри + defer</p></>,
    <><p>Фабрика операций над балансом + указатель</p></>,
    <><p>Variadic + несколько возвратов + defer лог количества</p></>,
    <><p>Замыкание, возвращающее функцию с defer внутри</p></>,
    <><p>Полный отчёт: variadic цены → сумма, скидка, итог + defer "Отчёт готов"</p></>,
    <><p>Указатель на счётчик + variadic добавления + defer вывод финала</p></>,
    <><p>Named returns + defer изменение + variadic вход</p></>,
  ]}
>

    <details>
        <summary>💡 Подсказка</summary>
        <p>Сочетайте всё: variadic для входных данных, указатель для изменения баланса, named returns для суммы и количества, defer для финального лога.</p>
        <p>Можно создать замыкание, которое использует указатель на баланс и добавляет суммы из variadic.</p>
        <p>Главное — показать, как все изученные механизмы работают вместе в одной программе.</p>
    </details>
</TaskWithVariants>

## Дополнительные материалы
После выполнения заданий:
- Поиграйтесь с порядком defer
- Создайте свои фабрики и замыкания
- Экспериментируйте с named returns

:::tip Совет
Функции — это строительные блоки. Делайте их маленькими, понятными и переиспользуемыми.
:::

:::info Следующий урок
Управляющие конструкции (if, for, switch).
:::
