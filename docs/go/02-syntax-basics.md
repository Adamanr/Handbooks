---
sidebar_position: 2
---

# Основы синтаксиса Go

Добро пожаловать во второй урок! Теперь, когда вы установили Go и написали свою первую программу, пора разобраться, как "говорит" этот язык. Мы пройдёмся по основным строительным блокам: переменным, типам данных, операторам и комментариям.

Всё будет с простыми аналогиями из жизни, множеством примеров и мини-заданиями, чтобы вы сразу пробовали код сами. Рекомендую открыть редактор (VS Code + Go extension) и повторять примеры по ходу чтения — так материал запомнится лучше.

## Структура программы на Go

Программа на Go похожа на рецепт блюда: есть ингредиенты (переменные), инструкции (операторы и функции) и упаковка (пакеты).

Основные элементы:
- **Пакеты** (`package`) — группируют код, как папки на компьютере.
- **Импорты** (`import`) — подключают готовые инструменты из стандартной библиотеки или других пакетов.
- **Функции** (`func`) — блоки кода, которые что-то делают.
- **Переменные и константы** — хранят данные.
- **Типы данных** — определяют, какого рода эти данные.

Каждая исполняемая программа начинается с пакета `main` и функции `main()` — это точка входа, как кнопка "Старт".

## Переменные и константы

Переменные — это именованные ячейки памяти, куда мы кладём данные. Их можно менять.

### Способы объявления переменных

```go
// 1. Полное объявление с типом
var name string = "Алексей"
var age int = 27

// 2. Без типа — Go сам догадается (type inference)
var city = "Санкт-Петербург"
var height = 178.5

// 3. Короткое объявление := (самый популярный способ внутри функций)
name := "Алексей"     // string
age := 27             // int
isDeveloper := true   // bool

// 4. Множественное объявление
var (
    firstName  string = "Мария"
    lastName   string = "Иванова"
    salary     float64 = 150000.0
    employed   bool    = true
)

// 5. Групповое короткое объявление
product, price, inStock := "Ноутбук", 89990.0, true
```

**Важно**: `:=` работает только внутри функций. Снаружи используйте `var`.

### Константы

Константы — значения, которые нельзя изменить. Объявляются через `const`.

```go
const Pi = 3.14159
const AppName = "Мой калькулятор"

const (
    HTTPStatusOK       = 200
    HTTPStatusNotFound = 404
    MaxRetries         = 5
)
```

Константы вычисляются на этапе компиляции — это делает код быстрее и безопаснее.

## Базовые типы данных

Go — статически типизированный язык: тип переменной известен во время компиляции.

### Числовые типы

| Тип       | Размер      | Диапазон                                      | Когда использовать                  |
|-----------|-------------|-----------------------------------------------|-------------------------------------|
| `int`     | 32/64 бита  | Зависит от платформы                          | Обычные целые числа                 |
| `int8`    | 8 бит       | -128..127                                     | Когда точно знаете маленький диапазон |
| `int16`   | 16 бит      | -32768..32767                                 |                                     |
| `int32`   | 32 бита     | -2.1 млрд..2.1 млрд                           |                                     |
| `int64`   | 64 бита     | Очень большой диапазон                        | Большие числа, время в наносекундах |
| `uint*`   | Без знака   | Только положительные                          | Счётчики, биты                      |
| `float32` | 32 бита     | ~7 знаков после запятой                       | Когда важна экономия памяти         |
| `float64` | 64 бита     | ~15 знаков после запятой (рекомендуется)      | Большинство расчётов с дробями      |

Пример:
```go
temperature := 23.5     // float64 по умолчанию
balance := 1000000      // int
byteValue := byte('A')  // uint8, равно 65
```

### Строки

Строки в Go — это последовательность байтов (UTF-8).

```go
greeting := "Привет, Go! 🚀"
multiline := `
Это многострочная строка.
Очень удобно для SQL-запросов
или длинных сообщений.
`

// Длина строки в байтах
length := len(greeting)  // посчитает байты, не символы!
```

### Булев тип

```go
isActive := true
hasError := false
canProceed := isActive && !hasError
```

### Нулевое значение (zero value)

Если переменную объявили, но не присвоили значение — Go даст "нулевое":

- `int` → 0
- `float` → 0.0
- `bool` → false
- `string` → ""
- указатели, слайсы, мапы → nil

Это спасает от случайных ошибок!

## Преобразование типов

Go не делает автоматическое преобразование (в отличие от JavaScript). Нужно явно указывать.

```go
age := 25
height := 1.75

// int → float64
total := float64(age) + height  // 26.75

// float64 → int (отбрасывает дробную часть!)
truncated := int(height)        // 1

// Число → строка
import "strconv"

ageStr := strconv.Itoa(age)               // "25"
price := 99.90
priceStr := strconv.FormatFloat(price, 'f', 2, 64)  // "99.90"

// Строка → число
num, err := strconv.Atoi("42")            // num = 42, err = nil
if err != nil {
    fmt.Println("Ошибка преобразования!")
}
```

## Операторы

### Арифметические

```go
a, b := 10, 3

fmt.Println(a + b)   // 13
fmt.Println(a - b)   // 7
fmt.Println(a * b)   // 30
fmt.Println(a / b)   // 3  (целочисленное деление)
fmt.Println(a % b)   // 1  (остаток)

a++  // a становится 11
b--  // b становится 2
```

### Сравнения

```go
x, y := 5, 10

x == y   // false
x != y   // true
x < y    // true
x <= y   // true
x > y    // false
x >= y   // false
```

### Логические

```go
hasTicket := true
hasPassport := false

canTravel := hasTicket && hasPassport   // false (И)
canEnter := hasTicket || hasPassport    // true  (ИЛИ)
notAllowed := !hasTicket                // false (НЕ)
```

### Присваивания

```go
score := 100
score += 20    // 120
score -= 10    // 110
score *= 2     // 220
score /= 5     // 44
score %= 7     // 2
```

## Комментарии

Комментарии помогают понять код через месяцы.

```go
// Однострочный комментарий — объясняем одну строку

/*
   Многострочный комментарий.
   Полезен для описания сложной логики
   или временного отключения кода.
*/

// TODO: реализовать кэширование результатов
// FIXME: исправить утечку памяти при большом трафике
// HACK: временное решение до обновления библиотеки
```

Go имеет инструмент `godoc` — он генерирует документацию из комментариев перед функциями/типами.

## Полноценный пример

```go
package main

import (
    "fmt"
    "strconv"
)

func main() {
    // Информация о человеке
    name := "Виктория"
    age := 22
    height := 168.3
    isStudent := true

    fmt.Printf("Имя: %s\n", name)
    fmt.Printf("Возраст: %d лет\n", age)
    fmt.Printf("Рост: %.1f см\n", height)
    fmt.Printf("Студент: %t\n", isStudent)

    // Небольшой расчёт
    nextYear := age + 1
    ageString := strconv.Itoa(nextYear)

    fmt.Println("В следующем году будет:", nextYear)
    fmt.Println("Возраст строкой:", ageString)

    // Проверка совершеннолетия
    isAdult := age >= 18
    fmt.Printf("Совершеннолетний: %t\n", isAdult)
}
```

Запустите этот код — увидите красивый вывод.

## Упражнения для закрепления

Выполните в отдельной папке. Проверяйте с помощью `go run`.

1. **Площадь прямоугольника**  
   Объявите переменные `length` и `width` (float64), вычислите и выведите площадь.

2. **Обмен значениями**  
   Создайте две переменные `a := 10`, `b := 20`. Поменяйте их значения местами **без** третьей переменной (подсказка: используйте арифметику).

3. **Простой калькулятор**  
   Объявите `num1` и `num2` (float64), выполните все четыре операции (+ − × ÷) и выведите результаты с пояснениями. Обработайте деление на ноль (проверьте `if num2 != 0`).

4. **Бонус: конвертер температуры**  
   Переведите температуру из Цельсия в Фаренгейт:  
   `f := c*9/5 + 32`  
   Выведите, например, "25°C = 77°F".

## Что дальше?

В следующем уроке разберём **функции** — как писать переиспользуемый код, передавать параметры и возвращать результаты. Там будет ещё интереснее: научимся разбивать программу на логические блоки.
